const DB_USER = 'ansua86';
const DB_PASSWORD = 'tjrrnddl1!';

const DB_URI = `mongodb+srv://${DB_USER}:${DB_PASSWORD}@cloud-pmbuu.mongodb.net/arsco_attendance`;

const KEY = 'leesukkyu';

module.exports = {
  DB: {
    DB_URI,
  },
  KEY,
};
